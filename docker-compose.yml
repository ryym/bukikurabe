version: '2'

services:
  db:
    image: postgres:9.6
    env_file: .docker-env-db
    volumes:
      - /etc/postgresql
      - /var/log/postgresql
      - /var/lib/postgresql
    ports:
      - '5433:5432'
  app:
    build: dockerfiles/app
    volumes:
      - .:/app
    ports:
      - '8080:8080'
    command: ["start"]
